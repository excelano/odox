# Values ship reads. Format and keys: anderix/ship, docs/DESIGN.md.
# `odox` last: it depends on `odox-core` and nothing depends on it.
targets=apt crates msix appstore
apt_package=odox
commands=xodt xods xodp odox
crates=odox-core odox-ui xodt xods xodp odox
display_name=odox
store_locales=en-us de-de

/target
/dist

# The Windows package identity Partner Center assigned. A public repository is
# not where an account's identifiers go, even the ones that are not secret;
# `packaging/windows/identity.psd1.example` is the committed template and says
# where the values come from.
/packaging/windows/identity.psd1
